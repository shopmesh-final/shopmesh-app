# ─── Order Service Environment ────────────────────────────────────────────

PORT=3003

# Local Development Mode
LOCAL_MODE=true

# AWS (only required when LOCAL_MODE=false)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# DynamoDB
DYNAMODB_ENDPOINT=http://dynamodb-local:8000
DYNAMODB_ORDERS_TABLE=shopmesh-orders

# SQS (optional in LOCAL_MODE; required when LOCAL_MODE=false)
SQS_ORDER_QUEUE_URL=
SQS_ENDPOINT=

# SNS Topic ARNs (only used when LOCAL_MODE=false)
SNS_ORDERS_TOPIC_ARN=arn:aws:sns:us-east-1:ACCOUNT_ID:shopmesh-orders
SNS_ALERTS_TOPIC_ARN=arn:aws:sns:us-east-1:ACCOUNT_ID:shopmesh-alerts

# Service URLs (inter-service communication)
AUTH_SERVICE_URL=http://auth-service:3001
PRODUCT_SERVICE_URL=http://product-service:3002

# JWT (LOCAL_MODE only; in AWS loaded from Secrets Manager via auth-service)
JWT_SECRET=local-dev-jwt-secret-change-in-production
