# ─── Auth Service Environment ─────────────────────────────────────────────

PORT=3001
NODE_ENV=development

# Local Development Mode (true = use DynamoDB Local + skip Secrets Manager)
LOCAL_MODE=true

# AWS (only required when LOCAL_MODE=false)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# DynamoDB (only required when LOCAL_MODE=true)
DYNAMODB_ENDPOINT=http://dynamodb-local:8000
DYNAMODB_USERS_TABLE=shopmesh-users

# JWT (only required when LOCAL_MODE=true; otherwise loaded from Secrets Manager)
JWT_SECRET=local-dev-jwt-secret-change-in-production
JWT_EXPIRES_IN=24h
